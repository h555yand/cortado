import { VariantSortPipe } from './variant-sort.pipe';

describe('VariantSortPipe', () => {
  it('create an instance', () => {
    const pipe = new VariantSortPipe();
    expect(pipe).toBeTruthy();
  });
});
